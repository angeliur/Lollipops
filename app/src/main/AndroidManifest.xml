<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.angeliur.lollipops">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".transactivity.TransActivity"/>
        <activity android:name=".transactivity.Transition"/>

        <activity android:name=".animatedselector.AnimatedSelectorActivity"/>

        <activity android:name=".recyclerview.RecyclerViewActivity"/>

        <activity android:name=".mylnotification.MyLNotificationActivity"/>

        <activity android:name=".palette.PaletteActivity" android:theme="@style/AppTheme2"/>

        <activity android:name=".toolbartest.ToolbarTestActivity" android:theme="@style/AppTheme2"/>
    </application>

</manifest>